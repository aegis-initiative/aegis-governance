verbose = true
exclude_mail = true
timeout = 20
max_retries = 2
retry_wait_time = 2
max_concurrency = 10

exclude = [
  "^http://localhost",
  "^https://localhost",
  "^http://127\\.0\\.0\\.1",
  "^https://127\\.0\\.0\\.1"
]

accept = [200, 206, 429]

exclude_path = [
  "archive/**"
]
